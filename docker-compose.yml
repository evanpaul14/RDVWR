services:
  rdvwr:
    build: .
    ports:
      - "8002:8002"
    environment:
      - REDDIT_OAUTH=${REDDIT_OAUTH:-1}
      - IMGUR_CLIENT_ID=${IMGUR_CLIENT_ID:-}
      - PROXY_MEDIA=${PROXY_MEDIA:-0}
    restart: unless-stopped
